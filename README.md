# cloud-workshops

A set of training docs and example code used for cloud workshops and training  

## Envoy Gateway 

The `envoy-gateway` subfolder contains training and example code for setting up Envoy Gateway for Kubernetes network traffic management on a CAPI cluster